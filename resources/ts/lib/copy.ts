/**
 * Shared user-facing copy. The Chat page and the floating widget must greet
 * the user with identical wording, so the strings live here rather than in
 * either component.
 *
 * @since v0.3.0
 */

/** Empty-state heading, shown before the first message of a conversation. */
export const WELCOME_TITLE = "What's up?";

/** Empty-state supporting line, shown beneath {@link WELCOME_TITLE}. */
export const WELCOME_SUB = "Ask anything. I'll take it from here.";
